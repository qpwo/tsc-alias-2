import { sum } from 'lodash-es'
// import { sum } from '../../../my-lodash-build.mjs'
// import sum from '../../../mymap.mjs'
import { middleVal } from '../middle'
console.log({ middleVal })
export const y = sum([1, 2, 3])

export function foo() {
    return 'foo'
}
